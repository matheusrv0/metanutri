# Modelos do estágio

Os arquivos `modelo_planejamento_estagio.docx` e `.txt` ficam **apenas na máquina local**: eles trazem o nome de uma preceptora real.

O MetaNutri usa só a **estrutura** desse modelo na exportação em Word (`src/export/aconselhamento-docx.ts`), sem nenhum nome de pessoa.
