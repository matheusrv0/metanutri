# dependências e build
node_modules/
dist/
coverage/

# testes e2e
test-results/
playwright-report/

# saídas geradas por ferramentas
graphify-out/
.playwright-mcp/

# ambiente
.env
.env.*
!.env.example
*.log
.DS_Store
Thumbs.db

# template de design de referência (arquivo grande, fica só local)
docs/design/referencia/*.zip
# export cru do design system: já foi incorporado em design-system/ (32 MB, fica só local)
design-system-export.zip

# modelo do estágio: contém nome de pessoa real, fica só local
docs/modelos/

# revisão visual do skill impeccable (capturas e mocks)
.impeccable/review/
.impeccable/mocks/
.env.local
.env

# capturas de conferência ficam fora do repositório
/*.png
